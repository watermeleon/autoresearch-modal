[project]
name = "tennis-xgboost-modal"
version = "0.1.0"
description = "Auto-research loop for tennis XGBoost prediction on Modal (CPU)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modal>=1.0.0",
]
